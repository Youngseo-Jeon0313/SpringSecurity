server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/koaladb
    username: root
    password: root

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: true

  mvc:
    view:
      prefix: WEB-INF/views/
      suffix: .jsp

  security:
    user:
      name: admin
      password: 2222